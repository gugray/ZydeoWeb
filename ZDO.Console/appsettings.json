{
  "logFileName": "/etc/zdo/console.log",
  "warehousePath": "_",
  // Dropbox uploader script
  "dropboxUploader": "_",
  "sites": [
    {
      "shortName": "hdd-live",
      "mutation": "HDD",
      "stagingof": "",
      // Site's display name in console
      "displayName": "HDD Live",
      // Root directory of deployed app
      "appRoot": "_",
      // Start/stop script
      "srvScript": "_",
      // App's root working directory
      "etcRoot": "_",
      // NGINX config file for online app
      "nginxPubCfg": "_",
      // NGINX config file for "maintenance" page (app offline)
      "nginxOffCfg": "_",
      // Dropbox folder for DB export backups
      "dropboxBackupFolder":  "_"
    }
  ]
}
